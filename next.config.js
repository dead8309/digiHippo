/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'digihippo-production.up.railway.app',
            'dead8309.shop'
          ],      
    }
}

module.exports = nextConfig
